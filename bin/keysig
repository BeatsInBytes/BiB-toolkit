#!/bin/bash

# Define the keysig function
keySigFinder() {
   echo $1
}


#lists all inputs
allArgList=$(echo "$@" | tr ' ' '\n' )

# Read the input file(s)
file=$(echo "$allArgList" | rev | cut -c 2- | rev | grep -v '-' | grep . )

echo "$file"
# Check number of files
numFile=$(echo "$file" | grep . | wc -l | tr -d ' ')

argList=$(echo "$@" | tr ' ' '\n'  )

# File number handling
if [ $numFile -eq '0' ]; then
   echo "   
   keysig: ERROR: No input file specified
   "
elif [ $numFile -gt '1' ]; then
   echo "
   keysig: ERROR: $numFile input files; only 1 supported
   "
else
   # echo "$argList" | tr '-' '+' | grep '+'
   echo done
   # keySigFinder $file 
fi

# for i; do 
#    if [ $i == '-h' ]; then
#       DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#       cat "${DIR}/man/keysig"
#    elif [ $i == '--help' ]; then
#       DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#       cat "${DIR}/man/keysig"
#    fi
# done