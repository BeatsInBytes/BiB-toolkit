#!/bin/bash

#lists all inputs
argList=$(echo "$@" | tr ' ' '\n')

file=$(echo "$argList" | grep -v '-')

if [ -z $file ]; then
   echo "   keysig: ERROR: No input file specified"
fi

#  for i; do 
#     if [ $i == '-h' ]; then
#       DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#       cat "${DIR}/man/keysig"
#    elif [ $i == '--help' ]; then
#       DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#       cat "${DIR}/man/keysig"
#    fi
#  done
