KEYSIG(1)

NAME
    keysig - finds key signatures in a kern file

SYNOPSIS
    keysig [-h] [--help] file...

DESCRIPTION
    corrupt modifies files by toggling a randomly chosen bit.

OPTIONS
    -n, --bits=BITS
        Set the number of bits to modify. Default is one bit.

KEYSIG(1)
